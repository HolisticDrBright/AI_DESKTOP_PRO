import { SurfaceScreen } from "@/components/ui/SurfaceScreen";
import { SURFACES } from "@/lib/surfaces";

export default function NutritionPage() {
  return <SurfaceScreen spec={SURFACES.nutrition} />;
}
